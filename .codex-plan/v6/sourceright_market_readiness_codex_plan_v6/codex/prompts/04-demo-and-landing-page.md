# 04 — Demo and landing-page polish

Polish existing demo surfaces and make them visible.

Tasks:
1. Inspect `github_pages_demo/` and `streamlit_app/`.
2. Ensure both use sample artifacts only.
3. Add clear demo disclaimers:
   - no live provider calls;
   - no API keys;
   - no mutation.
4. Add README/docs links to demos.
5. Add a three-minute demo script.
6. Add screenshots or sample report-card snippets if practical.
7. Do not introduce live network calls.
8. Run any available demo validation.

Do not deploy anything unless explicitly asked.
