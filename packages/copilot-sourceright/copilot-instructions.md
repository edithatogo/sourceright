# Sourceright + GitHub Copilot

Use the VS Code extension `edithatogo.sourceright` or merge `vscode-mcp.json` into Copilot MCP settings.

Run read-only diagnostics first (`sourceright report --json`). Do not claim write apply without explicit user approval.
