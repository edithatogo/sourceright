# Sourceright for GitHub Copilot

Copilot in VS Code can use the same MCP stdio server as Cursor:

1. Install `sourceright` CLI.
2. Merge `vscode-mcp.json` into your MCP config or install the VS Code extension from Track 83.

See `copilot-instructions.md` for repository instruction snippets.
