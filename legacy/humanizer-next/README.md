# Imported humanizer-next reference workflow

This directory preserves the reference, citation, and source-verification material imported from `..\humanizer-next`.

The import is intentionally broad. It includes the citation experiment, `humanizer-cite`, reference fixtures, source manifests, source-refresh notes, and archived Conductor tracks that mention citation or source verification.

Treat this as legacy source material. New implementation work should happen in the Sourceright core and use this directory for provenance, behaviour discovery, and regression fixtures.

