# CLI Plan

The planned command family is:

- `sourceright init`
- `sourceright extract`
- `sourceright normalize`
- `sourceright verify`
- `sourceright review`
- `sourceright export`
- `sourceright pipeline`
- `sourceright mcp`

Each command that returns structured data should support deterministic JSON output suitable for agents and CI.
