# Exports

CSL JSON is canonical. Sourceright should generate downstream files from verified, cleaned reference data:

- XML.
- ENW.
- RIS.
- BibLaTeX.
- YAML.

Exports should have stable ordering and structural validation. Where practical, export tests should reparse generated files.
