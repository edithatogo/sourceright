# CiteWeft repository extraction

Track 97 records the boundary and migration evidence for CiteWeft as an
independent repository. The published candidate is
`https://github.com/edithatogo/citeweft` at revision
`8c8932976250f9ca91c2bbda28ed68eeb191fa42`, while Sourceright retains adapters
such as GROBID and sidecar mapping.

The verifier checks the adapter boundary and external candidate metadata.
Independent CI/security/package proof and a disposable consumer smoke are
complete for the candidate. Stable release promotion, long-term dependency
migration, and rollback/issue-migration completion remain separately gated.
