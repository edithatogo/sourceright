# Metrics

Future benchmark runners should write local metric outputs here as ignored or
reviewed artifacts. Prefer JSON or CSV with task id, timestamp, command, exit
code, duration, and environment notes.
