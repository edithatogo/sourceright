# Demo Public Surface Proof Spec

Prove the GitHub Pages static demo and Streamlit local demo as separate
surfaces. The static demo needs render/browser smoke. The Streamlit demo needs
server startup and optional dependency behavior. Neither demo proves live
provider behavior.
