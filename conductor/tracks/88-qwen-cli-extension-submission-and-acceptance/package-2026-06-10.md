# Qwen CLI extension package — 2026-06-10

Package: `extensions/qwen-sourceright/` (`qwen-extension.json`, npm `@edithatogo/sourceright-qwen-extension`)

Install:

```bash
qwen extensions install https://github.com/edithatogo/sourceright
# or: qwen extensions install extensions/qwen-sourceright
```
