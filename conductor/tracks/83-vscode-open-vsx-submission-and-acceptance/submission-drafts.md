# Submission Drafts — VS Code and Open VSX Submission and Acceptance (Not Submitted)

Date: 2026-06-10

**Approval gate:** `vscode-open-vsx` has `external_submission_allowed: false` until this track completes readiness review.

## Draft target

VS Code Marketplace via `vsce publish`; Open VSX via `ovsx publish`.

## Artifact

VSIX `edithatogo.sourceright` with diagnostics/commands, publisher tokens, and public listing URLs.

## Rollback

Do not promote `submitted` or `publicly_accepted` gates without URL evidence in `live-evidence.json`.
