# CI Supply-Chain Hardening Plan

1. Add release smoke checks to CI.
2. Add dependency policy checks with `cargo-deny`.
3. Add docs and command smoke validation.
4. Add contributor documentation for local strict checks.
5. Keep expensive coverage/fuzz jobs scheduled or manual until runtime is stable.
