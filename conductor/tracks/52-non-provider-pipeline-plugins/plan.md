# Non-Provider Pipeline Plugins Plan

1. Inventory each non-provider plugin manifest and expected input/output contract.
2. Add deterministic fixtures for matching, recency/retractions, claim-source
   linkage, and extraction/OCR diagnostics.
3. Implement or explicitly defer each plugin.
4. Add docs and plugin status updates.
5. Run targeted tests and `$conductor-review`.
6. Apply local fixes and progress plugin lanes independently.
