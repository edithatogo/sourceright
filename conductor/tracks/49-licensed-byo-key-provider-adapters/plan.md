# Licensed BYO-Key Provider Adapters Plan

1. Define environment variables, redaction rules, and cache/privacy policy.
2. Add manifest validation for required auth configuration.
3. Add fixture-backed adapter tests using permitted sample payloads.
4. Add opt-in live smoke that refuses to run without explicit credentials.
5. Document licensing limitations and support status.
6. Run targeted tests and `$conductor-review`.
7. Apply local fixes; defer unavailable providers with exact access requirements.
