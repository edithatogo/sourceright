# Repository Record Provider Adapters Plan

1. Split the bundled repository-record manifest into sub-provider contracts.
2. Add fixtures for Zenodo, OSF, Figshare, Dataverse, and a generic institutional repository.
3. Normalize repository evidence into the sidecar evidence model.
4. Add URL/archive handoff points for track 39.
5. Update university repository docs and plugin status.
6. Run targeted tests and `$conductor-review`.
7. Apply local fixes and progress repository lanes independently.
