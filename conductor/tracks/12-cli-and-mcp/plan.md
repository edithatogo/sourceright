# CLI And MCP Plan

1. Define deterministic JSON output for each CLI command.
2. Implement pipeline command over the Rust core.
3. Implement MCP server mode from the same core.
4. Add smoke tests for CLI and MCP startup.
5. Add adapter package plans only after the core interfaces stabilize.
