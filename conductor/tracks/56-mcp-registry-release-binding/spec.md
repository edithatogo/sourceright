# MCP Registry Release Binding Spec

Validate that `server.json`, OCI image labels, release artifacts, checksums,
attestations, and official MCP Registry publication all bind to the same
Sourceright version.
