# CourtListener Legal Provider Spec

Implement CourtListener as a legal-citation provider for the separate legal
model. It writes `sourceright.legal_citation_report` evidence and must not
merge legal citations into academic CSL.
