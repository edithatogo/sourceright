# Submission Drafts — OpenCode Plugin Submission and Acceptance (Not Submitted)

Date: 2026-06-10

**Approval gate:** `opencode` has `external_submission_allowed: false` until this track completes readiness review.

## Draft target

npm plugin publish and/or OCX registry packument; MCP config template in repo.

## Artifact

OpenCode `mcp` config block, local plugin, or npm `plugin` entry with install smoke.

## Rollback

Do not promote `submitted` or `publicly_accepted` gates without URL evidence in `live-evidence.json`.
