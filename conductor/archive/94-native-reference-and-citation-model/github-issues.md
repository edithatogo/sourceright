# GitHub Issue Ledger

## Issue #39

Track 94 native reference and citation model. This slice delivers the
fixture-backed deterministic baseline and keeps learned runtime, calibration,
and independent cohort claims explicitly deferred.

Review fixes applied: added a 25 MiB pre-segmentation input limit and renamed
the extraction disposition to `extracted` so it cannot be confused with
bibliographic verification.
