# GitHub Issue Ledger

## Issue #42

Track 97 CiteWeft repository extraction. The local independence manifest and
verifier are implemented. External repository creation, history split, package
publication, downstream migration, and rollback remain approval-gated.

Review fixes applied: verifier and policy tests now enforce disjoint core and
adapter inventories, non-empty schema contracts, explicitly open external
gates, and expanded forbidden-import coverage.
