# Sourceright for OpenCode

1. Install Sourceright CLI (`sourceright` on `PATH`).
2. Copy `opencode.example.json` into `~/.config/opencode/opencode.jsonc` (merge `mcp` block).
3. Optional: `npm install @edithatogo/sourceright-opencode` and add to `plugin` array.

Restart OpenCode after editing config.
